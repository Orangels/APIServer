#
import os
import sys
import struct
import numpy as np


def add(a=0, b=1, c=2):
    print ("a = ", a, c, type(c))
    # b = np.frombuffer(b, dtype=c)
    print ("in python function add")
    print ("b = ", b)
    # print ("ret = ", a + b[0], type(b))
    # b[0] = 0
    # b[1] = 65537
    # b[2] = 0

    return a
